export function force(objectID, direction, _f){
    if(objectID && _f > 0){
        let _object   = _ObjectList[objectID];
        switch(direction){
            case "up":
                _object.physics
            break;
        }
    }
}